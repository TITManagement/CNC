# XYZ runner用 螺旋描画設定
driver: sim
defaults:
  unit: mm
  mode: absolute
  feed: 800

jobs:
  - type: gcode
    file: "examples/spiral.gcode"
    origin: [0, 0, 0]

visual:
  show: true
  title: "3D Spiral Pattern"
