# XYZ runner用 立方体描画設定
driver: sim
defaults:
  unit: mm
  mode: absolute
  feed: 1000

jobs:
  - type: gcode
    file: "examples/cube.gcode"
    origin: [0, 0, 0]

visual:
  show: true
  title: "3D Cube Pattern"
